apiVersion: v1
kind: Service
metadata:
  name: kuber-svc
  namespace: app
  labels:
    app: kuber-svc
spec:
  selector:
    env: prod
  ports:
    - protocol: TCP
      name: web
      port: 80
      targetPort: 8000
  type: LoadBalancer